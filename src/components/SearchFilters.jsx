import React from "react";

const SearchFilters = () => {
    return (
        <div className="d-flex align-items-center h-100">
            <input type="search" className="searchfilter" placeholder="Enter your filters..." />  
        </div>
    );
};

export default SearchFilters;  